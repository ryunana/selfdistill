# L3 用户画像（你是谁）

> 这一层写「关于你的事实」。
> 注意：健康、财务、家庭、身份细节等真实敏感信息请放入 `workspace/canonical/03-l3-private.md`（该文件不提交、默认不构建）；本文件只放可公开事实或明确标记为 `[SIMULATED]` 的演示数据。

## 基本信息

（姓名/称呼、职业、行业、所在地……）

## 经历

（工作经历、项目经历、时间线……）

## 偏好

（稳定偏好、习惯……）

## 真实敏感信息（不要放在本文件）

请新建本地 `workspace/canonical/03-l3-private.md` 保存真实健康 / 财务 / 家庭 / 身份细节。该文件默认不会进入 `dist/`；只有使用者明确执行 `python3 build.py --include-private` 时才用于本地写回。
