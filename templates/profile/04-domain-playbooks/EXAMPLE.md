---
schema_version: 1
id: EXAMPLE
description: "把这个 description 改成该 playbook 的触发场景，一句话英文（Use when ...）。"
---

# <领域名>

> 这一层写「你在某个领域可复用的工作方法」。每个领域一个文件。
> frontmatter 的 `id` 全局唯一（小写、连字符），`description` 写清什么场景触发。

## 核心方法

（该领域你沉淀的可复用方法论）

## 常见坑

（该领域你踩过的坑、经验教训）
